language: node_js
node_js:
  - "node"
services: 
  - mongodb
env:
  - TEST_DIR=back
  global:
    - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
script: cd $TEST_DIR && npm install && npm test-quick